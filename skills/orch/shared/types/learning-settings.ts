export interface LearningSettings {
  auto_record_memory?: boolean;
  auto_scan_on_finish?: boolean;
  auto_apply_safe?: boolean;
  min_samples?: number;
  recency_days?: number;
  dismiss_ttl_days?: number;
}
