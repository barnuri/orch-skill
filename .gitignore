# Typecheck symlink farm — populated from bun's global install cache, never committed.
.types/

# Editor / OS noise
.DS_Store
*.swp

# Dependencies and build output
node_modules/
dist/

# Browser-automation artifacts
.playwright-mcp/

# Agent session scratch
**/agent-spec

# Convenience symlink to the live state directory (~/.harness-orch): profiles.json, memory.json,
# runs/, jobs/. Per-machine and absolute, so it is created by hand and never committed.
/config
